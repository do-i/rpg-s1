/target/
/target

# Release staging: release.yml builds the tarball here, and so does a local
# rehearsal of that step.
/dist/

# Local work-in-progress planning documents.
/plans/

# Derived output from the external map editors. Existing tracked source
# backups remain tracked; newly generated backups and thumbnails do not.
*.tmx.bak
/assets/scenarios/*/.cache/
